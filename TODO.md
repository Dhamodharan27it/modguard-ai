# TODO

- [x] Fix `devvit.json` configuration for correct server + post asset dirs/entries.
- [x] Add a build script step to clean old `dist/` artifacts before build.
- [ ] Verify build output locations for frontend (`dist/`) and server (`dist/server/index.js`).
- [x] Run `npm run build` then `devvit playtest` to confirm dashboard updates.
- [ ] If still failing, remove any uploaded cached assets by incrementing app version and re-uploading.


