// Shim for @devvit/start/client — resolved by Vite at build time.
// This file exists only so TypeScript can type-check main.tsx.
import type { ReactElement } from 'react';

export declare function createClient(app: ReactElement): void;
